# Copy to frontend/.env for local dev. Never commit the real .env.
# Leave empty to use the Vite dev proxy (/api -> http://localhost:8000).
# Set to an absolute origin when not using the proxy, e.g. https://api.example.org
VITE_API_BASE=
